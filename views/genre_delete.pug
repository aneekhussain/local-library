extends layout 

block content 
    h1 Title: #{genre.name}

    p Do you want to delete this genre? 

    form(method="POST", action="")
      div.form-group
      input#genreid.form-control(type="hidden", name="genreid", required="true", value=genre._id)
    
      button.btn.btn-primary(type="submit") delete 